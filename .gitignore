.env
__pycache__/
*.pyc
.venv/
venv/
outputs/
*.log

# Real, company-named private submission deliverables - kept locally,
# delivered directly to the employer, never pushed to the public repo.
# Moved to /private/ so this file never has to spell out the real
# company name - the generic "Example Co." versions are intentionally
# tracked and public.
/private/
